# دليل نظام "إعلانات المنصة" (بانر/فيديو مستقل عن اشتراكات المطاعم)

تمت إضافة نظام إعلانات جديد **مستقل كليًا** عن نظام اشتراك البانر/الفيديو
الموجود مسبقًا (يلي مرتبط بكل مطعم عبر subscription.html). هذا النظام
الجديد إعلاناته تتحكم فيها إنت بس، كأدمن، بدون أي علاقة بمطاعم أو
اشتراكات — ما فيه دورة مراجعة، الإعلان يظهر فورًا بعد الإضافة.

---

## 1) وين تدير الإعلانات

لوحة الإدارة (`admin-dashboard.html`) → تبويب جديد **"📣 إعلانات المنصة"**.

من هناك تقدر:
- تضيف إعلان جديد: نوعه **بانر** (صورة) أو **فيديو** (رفع ملف أو رابط يوتيوب)،
  مع عنوان اختياري ورابط اختياري يفتح لما الزائر يضغط على الإعلان.
- تشوف كل الإعلانات المضافة، وتفعّل/توقف أي إعلان بضغطة زر (بدون حذفه)،
  أو تحذفه نهائيًا.

## 2) وين تظهر الإعلانات

كل إعلان **نشط** يظهر تلقائيًا (بدون أي تدخل من صاحب مطعم) في **مكانين**:

1. **الصفحة الرئيسية** (`home.html`) — قسم "إعلانات مميزة" للبانرات،
   وقسم "إعلانات فيديو" للفيديوهات.
2. **صفحة كل مطعم** (`restaurant-page.html`) — نفس الإعلانات (البانر
   والفيديو) تظهر فوق قسم "المنيو" مباشرة، بنفس المحتوى على كل صفحات
   المطاعم بدون استثناء.

لو ما فيه أي إعلان نشط من نوع معيّن، القسم المرتبط بيختفي تلقائيًا (لا
تظهر فراغات أو عناوين بدون محتوى).

## 3) الملف/الحقول الجديدة

مجموعة Firestore جديدة: `platformAds/{adId}`

| الحقل | الوصف |
|---|---|
| `type` | `"بانر"` أو `"فيديو"` |
| `title` | عنوان مختصر يظهر فوق/تحت الإعلان (اختياري) |
| `mediaUrl` | رابط الصورة (imgbb) أو الفيديو (Cloudinary أو يوتيوب) |
| `linkUrl` | رابط يفتح بصفحة جديدة لما يضغط الزائر على الإعلان (اختياري) |
| `target` | وين يظهر الإعلان: `"الرئيسية"` (الصفحة الرئيسية فقط) / `"المطاعم"` (صفحة كل مطعم فقط) / `"الكل"` (الاثنين معًا، وهو الافتراضي) |
| `status` | `"نشط"` أو `"غير نشط"` — بس "نشط" يظهر للزوار |
| `createdAt` | تاريخ الإضافة |

> اختيار "وين يظهر الإعلان" موجود بنفس نموذج الإضافة بلوحة الإدارة —
> كل إعلان بقدر يكون بمكان مختلف عن غيره (مثلاً إعلان بالرئيسية بس،
> وإعلان تاني بصفحة المطاعم بس، بنفس الوقت).

## 4) الأمان (Firestore Rules)

- القراءة عامة للجميع (`allow read: if true`) عشان الإعلانات تظهر حتى
  للزوار غير المسجلين على الرئيسية وصفحة المطعم.
- الكتابة (إضافة/تعديل/حذف) محصورة بحساب الأدمن فقط (`isAdmin()`) —
  لا صاحب مطعم ولا أي مستخدم آخر يقدر يلمس هاي المجموعة.

## 5) خطوات النشر

```bash
# لازم تنشر قواعد Firestore الجديدة عشان مجموعة platformAds تشتغل
firebase deploy --only firestore:rules

# نشر الصفحات المعدّلة/الجديدة
firebase deploy --only hosting
```

لا حاجة لأي تعديل على `functions` أو خطة Blaze لهذي الميزة — كل شي
يعمل مباشرة عبر الواجهة وقواعد الأمان.
