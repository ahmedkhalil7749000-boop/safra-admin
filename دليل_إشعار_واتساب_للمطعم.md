# دليل تفعيل إشعار واتساب لصاحب المطعم عند وصول طلب جديد (عبر UltraMsg)

هاي الميزة بتستبدل إشعار الـ Push المتصفح (لهاي الحالة بس) برسالة واتساب حقيقية
توصل صاحب المطعم على جواله مباشرة، عن طريق **UltraMsg** — خدمة بسيطة بتشتغل
بربط رقم واتساب عادي عن طريق مسح كود QR (متل واتساب ويب)، بدون توثيق أعمال
أو بطاقة ائتمان.

## 1. أنشئ حساب UltraMsg

1. روح لـ https://ultramsg.com وسجّل حساب جديد بإيميلك العادي.
2. بعد التسجيل رح تدخل على لوحة التحكم (Dashboard).

## 2. أنشئ Instance واربطه برقم واتساب

1. من لوحة التحكم، أنشئ **Instance** جديد (هاد بمثابة "الخط" يلي رح ترسل منه).
2. رح يظهرلك **QR Code** — افتح تطبيق واتساب على الجوال يلي بدك ترسل منه
   (ممكن يكون جوالك الشخصي كبداية للتجربة)، روح على:
   **الإعدادات → الأجهزة المرتبطة → ربط جهاز**، وامسح الكود.
3. بعد المسح، الـ Instance بيصير "متصل" (Connected) وجاهز يرسل رسائل.

## 3. خذ بيانات الاعتماد

من صفحة الـ Instance رح تلاقي:
- **Instance ID** (شكله مثلاً `instance123456`)
- **Token**

هاي القيم بدنا نخزّنها بأمان بمشروع Firebase (مو بالكود مباشرة).

## 4. خزّن البيانات كـ Secrets بمشروع Firebase

من داخل مجلد المشروع، بالـ Terminal:

```bash
firebase functions:secrets:set ULTRAMSG_INSTANCE_ID
# الصق قيمة Instance ID واضغط Enter

firebase functions:secrets:set ULTRAMSG_TOKEN
# الصق قيمة Token واضغط Enter
```

هاي القيم بتنخزن مشفّرة بـ Google Secret Manager، وما بتظهر بالكود أبداً.

## 5. نشر الدوال

```bash
firebase deploy --only functions
```

(ما نحتاج `npm install` مكتبة إضافية — UltraMsg بيشتغل عن طريق طلب HTTP
عادي، وNode 20 المستخدم بالمشروع فيه `fetch` جاهز مسبقاً.)

## 6. لازم يكون عند صاحب المطعم رقم هاتف مسجّل

الرسالة بتنرسل عرقم الهاتف المخزّن بوثيقة `users/{uid}.phone`، وهاد الحقل
بينعبّى من صفحة **"حسابي" (my-account.html)**. تأكد إن كل صاحب مطعم
دخّل رقمه فيها (بصيغة محلية زي `079xxxxxxx` وبينحول تلقائياً لصيغة
دولية بدون + مثل `9627xxxxxxx`).

لو بدك تغيّر كود الدولة الافتراضي (حالياً `962` للأردن)، عدّل القيمة
`DEFAULT_COUNTRY_CODE` بأول ملف `functions/index.js`.

## كيف تشتغل الميزة بعد النشر

- أي طلب جديد ينعمل بمجموعة `orders` → صاحب المطعم ياخد رسالة واتساب
  فوراً فيها قيمة الطلب، رقمه، وموعد التجهيز لو كان طلب مسبق.
- ما عاد يوصله إشعار Push متصفح لهاي الحالة تحديداً (حسب طلبك) - باقي
  الإشعارات (تغيّر حالة الطلب للزبون، تذكير الاشتراك، إلخ) لسا شغالة
  بنفس طريقة الـ Push متل قبل.

## ملاحظة مهمة

UltraMsg بيستخدم بروتوكول واتساب ويب (مش WhatsApp Business API الرسمي
من ميتا)، فهاي طريقة **غير رسمية**:
- ممكن واتساب يوقف الرقم مؤقتاً لو أرسلت رسائل كتير بوقت قصير جداً.
- الجوال يلي مسحت منه الكود لازم يضل متصل بالإنترنت بين فترة وأخرى
  (متل واتساب ويب بالضبط) عشان الربط يضل شغال.
- مناسبة تماماً لحجم استخدام صغير-متوسط (مطعم أو عدة مطاعم)، بس لو
  المشروع كبر كتير لاحقاً، الأفضل تنتقل لـ WhatsApp Business API الرسمي.
