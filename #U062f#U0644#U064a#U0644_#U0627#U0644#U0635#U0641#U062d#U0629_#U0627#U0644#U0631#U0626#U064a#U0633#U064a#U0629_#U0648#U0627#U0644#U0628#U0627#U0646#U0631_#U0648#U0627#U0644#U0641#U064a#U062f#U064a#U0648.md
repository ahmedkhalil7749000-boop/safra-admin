# دليل الصفحة الرئيسية الجديدة + نظام البانر والفيديو — منصة سفرة

تمت إضافة صفحة رئيسية حقيقية للمنصة، مبنية على **3 أنواع اشتراك مستقلة تمامًا
عن بعض**: الاشتراك الأساسي (موجود مسبقًا)، اشتراك البانر، واشتراك الفيديو.
كل نوع إله دورة اشتراك (طلب → CliQ → سكرين شوت → مراجعة أدمن → تفعيل)
منفصلة بالكامل عن الأنواع التانية — فمطعم ممكن يكون عنده اشتراك أساسي فعّال
واشتراك بانر منتهي واشتراك فيديو قيد المراجعة بنفس الوقت.

---

## 1) الملفات الجديدة / المعدّلة

| الملف | التغيير |
|---|---|
| `home.html` | **جديد** — الصفحة الرئيسية الفعلية: مطاعم مميزة (الأعلى تقييمًا)، بانرات معتمدة، فيديوهات مطاعم معتمدة |
| `index.html` | صار يحوّل تلقائيًا لـ `home.html` بدل `restaurants.html` |
| `shared-header.js` / `shared-bottom-nav.js` | رابط "الرئيسية" صار يوجّه لـ `home.html` |
| `subscription.html` | أعيد بناؤها بالكامل — 3 تبويبات (أساسي / بانر / فيديو)، كل وحدة إلها دورة اشتراك مستقلة |
| `restaurant-dashboard.html` | زر جديد "📣 البانر والفيديو" بكل بطاقة مطعم: رفع صورة بانر أو رابط فيديو (يظهر بس لو الاشتراك المناسب نشط)، وعرض حالة المراجعة |
| `admin-dashboard.html` | تبويب جديد "الرئيسية": اعتماد/رفض البانرات والفيديوهات، وقائمة "المعتمد حاليًا". كمان تبويب "الاشتراكات" صار فيه فلتر حسب النوع، ونموذج إضافة خطة صار فيه اختيار النوع |
| `firestore.rules` | تشديد صلاحيات `restaurants`: صاحب المطعم يقدر يرفع/يغيّر `bannerUrl`/`videoUrl` بحرية، لكن ممنوع عليه يحط `bannerStatus`/`videoStatus` = "معتمد" بنفسه — هاي محجوزة للأدمن فقط (تحقّق حقيقي بقواعد الأمان، مو بس بالواجهة) |
| `functions/index.js` | دالة جديدة `onRestaurantPromoStatusChangedNotifyOwner` — إشعار Push تلقائي لصاحب المطعم عند اعتماد/رفض بانره أو فيديوه |

---

## 2) حقول جديدة على وثيقة المطعم (`restaurants/{id}`)

| الحقل | الوصف |
|---|---|
| `bannerSubscriptionStatus` / `bannerSubscriptionEndDate` | حالة/تاريخ انتهاء اشتراك البانر (نفس قيم الأساسي: قيد المراجعة / بانتظار كود التفعيل / نشط / منتهي / مرفوض) |
| `bannerUrl` / `bannerStatus` / `bannerRejectReason` | صورة البانر التي رفعها صاحب المطعم، وحالة مراجعتها من الأدمن (قيد المراجعة / معتمد / مرفوض)، وسبب الرفض إن وجد |
| `videoSubscriptionStatus` / `videoSubscriptionEndDate` | نفس الفكرة لاشتراك الفيديو |
| `videoUrl` / `videoStatus` / `videoRejectReason` | رابط فيديو المطعم (يوتيوب أو رابط مباشر) وحالة مراجعته |

> ملاحظة: الاشتراك الأساسي احتفظ بنفس اسمي الحقلين القديمين
> `subscriptionStatus` و `subscriptionEndDate` بدون أي تغيير، حفاظًا على
> التوافق مع البيانات الموجودة مسبقًا.

## 3) حقل جديد على `subscriptionPlans` و `subscriptions`: `type`

القيمة تكون واحدة من: `"أساسي"` / `"بانر"` / `"فيديو"`. أي خطة أو طلب
اشتراك قديم (اتسجل قبل هالتحديث) ما فيه هالحقل أصلاً — الكود بكل الصفحات
يتعامل معه تلقائيًا كـ `"أساسي"` (`p.type || "أساسي"`)، فما في داعي لأي
تعديل يدوي على بيانات قديمة.

---

## 4) خطوة أولى لازمة: أضف خطط "بانر" و"فيديو"

زي ما هو الحال مع الاشتراك الأساسي، ما في خطط بانر/فيديو محفوظة افتراضيًا.
روح للوحة الإدارة → تبويب **الاشتراكات** → **إدارة خطط الاشتراك**، واختر
النوع من القائمة المنسدلة الجديدة، مثلاً:

- نوع **بانر**: "بانر شهري" — 5 د.أ — 30 يوم
- نوع **فيديو**: "فيديو شهري" — 7 د.أ — 30 يوم

بدون خطة واحدة فعّالة على الأقل من كل نوع، تبويب هالنوع بصفحة
`subscription.html` رح يظهر لصاحب المطعم رسالة "ما في خطط متاحة حاليًا".

---

## 5) كيف تشتغل الدورة بالكامل (بانر أو فيديو — نفس الخطوات)

1. صاحب المطعم يفتح **subscription.html** ويختار تبويب "بانر" أو "فيديو".
2. يختار خطة، يحوّل عبر CliQ، يرفع سكرين شوت → طلب بحالة "قيد المراجعة".
3. الأدمن من تبويب **الاشتراكات** يفعّل الطلب (مباشر أو بكود) — هذا يفعّل
   **الاشتراك نفسه** (`bannerSubscriptionStatus`/`videoSubscriptionStatus`
   = "نشط")، وليس المحتوى بعد.
4. بعد ما الاشتراك يصير نشط، يظهر لصاحب المطعم بلوحته زر "📣 البانر
   والفيديو" — يرفع صورة بانر أو يكتب رابط فيديو.
5. هالمحتوى يدخل تلقائيًا بحالة "قيد المراجعة" (`bannerStatus`/`videoStatus`).
6. الأدمن من تبويب **الرئيسية** الجديد بلوحة الإدارة يشوف الطلب، ويضغط
   "اعتماد" أو "رفض" (مع سبب اختياري).
7. بس لما تصير الحالة "معتمد" **و** الاشتراك المرتبط لسا نشط وما انتهت
   مدته، البانر/الفيديو يظهر تلقائيًا بـ `home.html` لكل الزوار.

> إذا انتهت مدة اشتراك البانر أو الفيديو، المحتوى بيختفي تلقائيًا من
> الصفحة الرئيسية (حتى لو حالة المراجعة لسا "معتمد") لحد ما يجدد الاشتراك.

---

## 6) قسم "مطاعم مميزة" بالصفحة الرئيسية

هذا القسم **مستقل تمامًا عن الاشتراكات** — بيعرض أعلى 8 مطاعم تقييمًا
(`rating`) من بين كل المطاعم المفتوحة حاليًا، بغض النظر عن وجود أي اشتراك
بانر أو فيديو. الهدف منه إبراز جودة الخدمة الفعلية، لا مين دافع أكتر.

---

## 7) خطوات النشر (Deploy)

```bash
# 1) قواعد Firestore (لازم تنشرها عشان تشديد صلاحيات bannerStatus/videoStatus يشتغل)
firebase deploy --only firestore:rules

# 2) الاستضافة (كل الملفات الجديدة/المعدّلة: home.html, subscription.html, ...)
firebase deploy --only hosting

# 3) دالة إشعار اعتماد/رفض البانر والفيديو (تحتاج خطة Blaze — اختيارية)
firebase deploy --only functions
```

> نفس ملاحظة النظام الأساسي: باقي الميزة (رفع البانر/الفيديو، المراجعة،
> الظهور بالرئيسية) بتشتغل 100% حتى بدون نشر functions — الإشعار
> التلقائي بس هو يلي محتاج خطة Blaze.

---

## 8) ملاحظة أمان

التحقق من إن صاحب المطعم ما يقدر "يعتمد" بانره أو فيديوه بنفسه (تجاوز
مراجعة الأدمن) متحقق فعليًا داخل `firestore.rules` (مو بس بالواجهة) —
صاحب المطعم مسموح له يحط `bannerStatus`/`videoStatus` بس على قيمة "قيد
المراجعة"، وأي قيمة ثانية (زي "معتمد") مرفوضة من قواعد الأمان نفسها إلا
إذا كانت جاية من حساب الأدمن.
